---
import MainLayout from '../../layouts/MainLayout.astro';
import ReportPage from '../../components/reports/ReportPage';
import type { UserRole } from '../../types/userRole';

// We'll handle authentication in client-side
// This is just a placeholder for SSR
const isAuthenticated = true;
const userRole: UserRole | null = null;
---

<MainLayout title="Work Order Reports" isAuthenticated={isAuthenticated} userRole={userRole}>
  <ReportPage client:load />
</MainLayout>
